# Hotel Reservation App

## Setup Instructions
1. Create an `.env` with the specified variables from `.env.example`.
    **IMPORTANT**: Make sure that git is not tracking this file so that it is not pushed to the repository. 
2. Connect to McGill's VPN if not connected to McGill wifi before running the program.